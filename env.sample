# Diese Datei zu `.env` kopieren und Werte setzen (nur falls ihr Cloud-Dienste nutzt).
# Die Notebooks lesen die Variablen mit `os.environ.get(...)`.

# MongoDB (Atlas empfohlen)
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGO_DB=workshop_mongo

# Neo4j (Aura empfohlen)
# Falls euer Notebook eine URI nutzt, setzt zusätzlich:
# NEO4J_URI=neo4j+s://<id>.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=<password>

# Lokale Fallbacks (optional)
# MONGO_URI=mongodb://localhost:27017
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=test1234
